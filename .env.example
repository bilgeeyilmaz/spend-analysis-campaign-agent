# Bu dosyayı `.env` olarak kopyalayıp değerleri doldur. `.env` asla commit edilmez.

# Groq API anahtarı — https://console.groq.com/keys
# Boş bırakılırsa sistem otomatik olarak deterministik yedek moda düşer (demo yine çalışır).
GROQ_API_KEY=
